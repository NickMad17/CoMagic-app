import {AppLayout} from "@/shared";
import {UsersList} from "@/widget/UsrsList";

const HomePage = () => {
    return (
        <AppLayout>
            <UsersList/>
        </AppLayout>
    );
};

export default HomePage
